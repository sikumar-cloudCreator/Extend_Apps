# secrets — NEVER commit
.env
.env.*
*.key

# python
.venv/
__pycache__/
*.pyc

# runtime state / learning store (machine-local)
feedback/

# build artifacts / generated bundles
out_app/
*.zip
*.docx
*.bak

# customer deliverables — this repo is the BUILDER only, never a built app or a
# customer's data. Built pages/bundles, per-customer analyses and fixtures stay local.
examples/seller_dashboard/
docs/seller_*


# os
.DS_Store
SellerPerformance_app/
*.db

knowledge/seller_*
docs/production_generator_design.md

gate/datasources.tenant.json
gate/datasources.demo.json
evals/golden.local/
